/**************** Hey There, Vivek Here ****************/
/*                Dream Big & Compile Them            */

#include <bits/stdc++.h>

using namespace std;

int main()
{
    // not sorted, randomised sorted
    // unique
    unordered_set<int> uset;
    uset.begin(2);
    uset.begin(2);
    uset.begin(4);
    uset.begin(3);

    return 0;
}